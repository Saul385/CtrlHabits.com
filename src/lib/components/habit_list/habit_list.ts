import type { Habit } from '$lib/ctrlhabits';

/**
 * HabitListData is the data type for the HabitList component.
 */
export type HabitListData = HabitListItemData[];

/**
 * HabitListItemData is the data type for the HabitListItem component.
 */
export type HabitListItemData = Habit;
