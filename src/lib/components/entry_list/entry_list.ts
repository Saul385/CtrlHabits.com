import type { Entry } from '$lib/ctrlhabits';

/**
 * EntryListData is the data type for the EntryList component.
 */
export type EntryListData = EntryListItemData[];

/**
 * EntryListItemData is the data type for the HabitListItem component.
 */
export type EntryListItemData = Entry;
